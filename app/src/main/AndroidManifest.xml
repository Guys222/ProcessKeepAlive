<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 11+ 包可见性：申请全量包可见权限，可列出所有已安装应用（含无桌面图标应用） -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- 常驻通知：Android 13+ 的通知运行权限 + 前台服务（14 起需类型权限） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 开机后恢复常驻通知（真正的保活不依赖它） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ★ 检查更新：仅用于访问 GitHub releases API 查询最新版本号与下载地址。
         本模块【不】接入任何第三方统计/推送 SDK，除用户主动点「检查更新」外不发起任何网络请求。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 兜底：在不授予 QUERY_ALL_PACKAGES 时，仍可列出带桌面图标（LAUNCHER）的应用 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:description="@string/app_desc"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 能力自检详情页：逐项展示每个保活 hook 的挂载结局（命中/未命中）。
             独立成页而非弹窗，是为了能逐项展开明细、给未命中项排查提示。 -->
        <activity
            android:name=".CapabilityDiagActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- 向 system_server 暴露配置的 ContentProvider（跨进程读配置） -->
        <provider
            android:name=".ConfigProvider"
            android:authorities="io.github.guys222.processkeepalive.config"
            android:exported="true" />

        <!-- 常驻状态：前台服务 + 常驻通知。
             target 34 起必须声明 foregroundServiceType，specialUse 需配套 purposes 说明。 -->
        <service
            android:name=".GuardService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="在通知栏与桌面小部件显示本模块保活目标的实时守护状态，供用户随时查看，无任何后台数据收集行为。" />
        </service>

        <!-- 通知上的「关闭常驻」按钮 -->
        <receiver android:name=".StopReceiver" android:exported="false" />

        <!-- 开机完成后按开关恢复常驻通知 -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 桌面小部件 -->
        <receiver
            android:name=".GuardWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/guard_widget_info" />
        </receiver>

        <!-- LSPosed / Xposed 模块声明 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Android 后台保活模块：防止目标应用被系统杀死" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>

</manifest>
