# 正式包说明（v3.3.0 release）

**产物**：`ProcessKeepAlive-v3.3.0-release.apk`
**包名**：`io.github.guys222.processkeepalive`（与线上一致，LSPosed 作用域无需重勾）
**versionCode**：4000 · **versionName**：3.3.0（无 debug 后缀）
**大小**：4.8 MB
**MD5**：`c8c06da6bdf3a8c53616ea438c308b68`
**SHA-256**：`799e02d3b59a9d006e26980bc4fb590f14989e7b7f17faac766651dea4df5c12`

> **命名说明**：正式包【不使用】debug 那套递增序号（001→062）。
> 序号是 debug 测试包的机制（版本号长期固定 3.3.0，只能靠文件名分新旧）；
> 正式包直接以版本号命名，与线上历史包一致：
> `ProcessKeepAlive-v3.2.1-release.apk` / `v3.2.2` / **`v3.3.0`**。

---

## 签名信息（已验证）

| 项 | 值 |
|---|---|
| 签名者 DN | `CN=Guys222, OU=ProcessKeepAlive, O=Personal, C=CN` |
| 证书 SHA-256 | `DF:25:46:E1:37:DE:ED:F0:71:19:8A:87:BA:27:88:47:7C:BC:49:91:E9:20:BC:FD:12:EA:99:0C:31:8C:69:F1` |
| 证书 SHA-1 | `B1:86:B9:FF:C0:FD:22:D6:FB:BF:EA:35:BA:85:14:96:6D:A6:46:3E` |
| 有效期 | 至 2051-09-10 |
| 签名方案 | v2 ✅ + v3 ✅（v1 被 AGP 优化掉，见下） |
| 密钥文件 | `ProcessKeepAlive-release-key.jks`（PKCS12 格式） |

**已用 `apksigner verify` 确认签名真实生效**，不是未签名包。

### 关于 v1 签名为 false

`build.gradle` 里写了 `enableV1Signing true`，但 `apksigner` 报 v1 = false。
原因：**AGP 8.2.2 在 minSdk ≥ 24 时会主动忽略 v1 签名**（v1/JAR 签名对 Android 7.0+ 无必要）。
本项目 minSdk 26（Android 8.0），v2 + v3 完全够用，**不影响任何设备安装**。已在 `build.gradle` 注释中说明，避免后续误判为 bug。

---

## 签名指纹核对结果（重要）

排查中发现**三把不同密钥**，指纹如下：

| 来源 | SHA-256 |
|---|---|
| 020 交接清单记录（写"历代一致、可覆盖安装"） | `9CFE46B7…8001D8` |
| **本次使用的 release-key.jks** | `DF:2546E1…8C69F1` |
| 沙箱自动生成的 debug keystore | `14:83:FF:E5…7C1F` |

**本次按您的决定使用 `release-key.jks`（`DF:2546E1…`）。**

⚠ **若线上 v3.2.2 是用其它密钥签的，老用户安装本包会报"签名冲突"，需卸载重装（配置会丢）。**
建议在 Release 说明里加一句：*"如遇签名不一致无法覆盖安装，请卸载旧版后重装。"*

---

## 本版内容（v3.3.0）

### 新增：检查更新（手动）

设置页「版本」行改为可点击：

- **点击** → 请求 GitHub releases API 查询最新版本，与本地比对：
  - 有新版：状态显示「有新版 vX.Y.Z」，弹窗询问是否前往下载（跳 GitHub releases 页）
  - 已最新：状态显示「已是最新」
  - 失败：状态显示「检查失败」+ 简短原因（网络不可用 / 请求过于频繁 / 尚无正式发布版）
- **长按** → 复制当前版本号（与「关于」区其它行交互一致）

**新增 `INTERNET` 权限**，用于访问 GitHub API。设计上刻意保持最小：

- 只在用户**主动点击**时发请求，无后台轮询、开机不联网
- 只访问 `api.github.com` 一个域名，**不引入任何第三方 SDK**
- 失败一律静默降级，不弹错误弹窗
- 零第三方依赖（JSON 用字符串定位截取，不引入 Gson/org.json）

实现类：`UpdateChecker.java`（独立于 UI）。
版本比较逻辑已通过 14 项单测，含 `v3.10.0 > 3.9.0` 这类字符串比较会判错的用例。

### 功能变更（同 061）

1. **消息保活文案点明**：摘要行改为「豁免省电限制 · 伪装前台防断连（改进程状态，非档位改 adj）」；
   展开详情含"与保活档位的区别"整段。
2. **057 屏蔽逻辑已全部回滚**：消息保活与保活档位现在可同时开启、互不覆盖
   （UI 3 处 + hook 5 处 + 标签 1 处全部放开）。

---

## 与 debug 包的区别

| | debug 测试包 | release 正式包 |
|---|---|---|
| 文件名 | `ProcessKeepAlive-v3.3.0-debug-20260918-0935.apk` | `ProcessKeepAlive-v3.3.0-release.apk` |
| versionCode | 3063 | **4000** |
| versionName | `3.3.0-debug-20260918-0935` | `3.3.0` |
| 签名 | 沙箱 debug keystore | **正式 release 密钥** |
| 体积 | 5.8 MB | 4.8 MB |
| 可调试 | 是 | 否 |
| 混淆 | 无 | **也无**（`minifyEnabled false`） |

### 命名与版号约定（本次统一）

**命名**：两者都改用**版本号体系**，不再用 `001~062` 递增序号
（序号机制要跨目录扫描维护，易错位，且与正式包命名不一致）：

```
ProcessKeepAlive-v3.3.0-debug-20260918-0935.apk   ← debug（时间戳区分先后）
ProcessKeepAlive-v3.3.0-release.apk                ← 正式（版本号即唯一标识）
```

**versionCode 分段**（写死在两个构建脚本里，避免撞号）：

| 区间 | 用途 |
|---|---|
| 3000~3999 | debug 测试包（递增） |
| **4000+** | **正式发布包** ← 恒大于 debug，保证「先装 debug 再装正式」时正式包永远被认作最新 |

> 历史遗留：旧 debug 用过 `3000+序号`，062 恰好是 3062，与上一版正式包撞号。
> 现正式包提到 4000 段，彻底分开。（发新版时改 `build-release.sh` 里的值，
> 如 v3.3.1 → 4001。）

> **注意**：release **未开混淆**。这对反射密集的 Xposed 模块是正确选择——
> 本项目大量按字段名反射（`findFieldLike` 扫描、`invokeInt(tc, target, ...)`），
> 一开混淆就会把反射依赖的字段名改掉、导致 hook 静默失效。
> 所以 release 与 debug 功能等价，只差签名与包信息。

---

## 新增构建脚本 `build-release.sh`

正式包用此脚本构建（与 `build-debug.sh` 同一套序号机制）：

```bash
cd /workspace/ProcessKeepAlive && bash build-release.sh
```

- **强校验**：缺 `key.properties` 或 keystore 直接拒绝构建（正式包绝不能未签名）
- 产物命名：`0NN-ProcessKeepAlive-v3.3.0-release.apk`
- 构建后自动 `apksigner verify` 打印签名指纹，防止"未签名当正式包发"
- versionCode 沿用 3000+序号机制（本包 3062）

---

## 待办

- [ ] 确认线上 v3.2.2 的签名是否为 `DF:2546E1…`（如不是，发布说明需加"需卸载重装"提示）
- [ ] 发布到 GitHub Release（tag 建议 `v3.3.0`）
